# Ignore the output folder and the output file so we don't loop
notebook_pages/
information/
notebook.html
release/

# GENERATED from eleventy_njk/{home,music,about,contact}.njk. Listed here for
# the same reason as notebook.html: input and output are both the repo root, so
# Eleventy must not read its own output back in as a template.
index.html
music.html
about.html
contact.html

# Ignore your static files so Eleventy doesn't try to process them
404.html

# GENERATED from eleventy_njk/download.njk. Same reason as index.html above:
# input and output are both the repo root, so without this the watcher would see
# Eleventy's own output land in the input dir and rebuild in a loop.
download.html

# Hash cache written by eleventy_njk/download.11tydata.js. Ignored so writing it
# can never retrigger the watcher (see the search-index.json note below).
eleventy_njk/download_hashes.json

# GENERATED from eleventy_njk/discography.njk. Same reason as index.html above.
discography.html

# legal.html is GENERATED from input_legal/legal.md — listed here because the
# input and output directories are both the repo root, so without this Eleventy
# would read its own output back in as a template.
legal.html

# Raw license texts + their sidecars are data, not pages. Without this the
# README.md in there would inherit input_legal.11tydata.js's permalink and fight
# legal.md over legal.html.
input_legal/licenses/

# Ignore assets/configs
node_modules/
revery_notebook/
# A standalone browser app, not content. Without this every .md inside it is
# rendered as a public page — .claude/skills/verify/SKILL.md was being published
# to /rvry_ascii/.claude/skills/verify/SKILL/index.html.
rvry_ascii/
licence/
package.json
package-lock.json
CLAUDE.md
update_to_git_guide.md
page_builder/
page_builder_app_v2/
input.css
main.css
prose.css
eleventy.config.js
README.md
color_theme_app/
eleventy_binary/

# GENERATED by eleventy_njk/search-index.njk. Must be ignored: input and output
# are both the repo root, so this file lands inside the input dir, and Eleventy's
# watcher treats a root-level .json as a data file. Without this line every
# `npm start` rebuild rewrites it, which fires the watcher, which rebuilds —
# an endless loop (~19 rebuilds/5s, measured). The file is still generated
# normally; ignoring it only keeps Eleventy from reading its own output back in.
search-index.json

# The reference site checkout, kept temporarily for comparison. It is a WHOLE
# OTHER Eleventy project: its eleventy_njk/*.njk use filters that only its own
# config defines, so without this line the build dies with "filter not found:
# absolute" on website_hrldthrslnd/eleventy_njk/feed.njk. Delete this entry
# together with the folder.
website_hrldthrslnd/
