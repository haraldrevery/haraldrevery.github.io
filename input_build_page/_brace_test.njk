---
title: Brace Torture Test
date: 2026-09-06
tags: [utility]
description: Regression fixture. Proves the exported body is never parsed as a template.
draft: true
---
<!-- REGRESSION FIXTURE — keep. Not published (draft: true).

     Every sequence below is a Nunjucks construct. If templateEngineOverride is
     ever dropped from input_build_page.11tydata.js, Nunjucks parses this body
     and the build dies here instead of silently mangling a real post.

     To check by hand: set draft to false, build, and confirm every sequence
     below appears LITERALLY in notebook_pages/_brace_test.html. Then set it
     back to true. -->
<div class="page-container pt-24 pb-12">
  <p>Output tag, must survive verbatim: {{ CONTENT }}</p>
  <p>Undefined variable, would render empty if parsed: {{ nothing_here }}</p>
  <p>Statement tag: {% if x %}visible{% endif %}</p>
  <p>Comment open: {# and a close: #}</p>
  <p>Raw block markers: {% raw %} and {% endraw %}</p>
  <p>KaTeX display math: $$\frac{{a}}{{b}}$$</p>
  <p>KaTeX inline: \(\sqrt{{x}}\)</p>
  <p>A CSS-in-HTML brace run: <code>@media{.a{color:red}}</code></p>
  <p>Template literal in JS: <code>`${foo}`</code> and <code>${{bar}}</code></p>
</div>
